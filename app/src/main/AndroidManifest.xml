<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Required for reading installed apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- Required to set wallpaper -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <!-- Required to read wallpaper -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <!-- ============================================================
             MAIN LAUNCHER ACTIVITY
             The HOME + DEFAULT intent filters register this as a launcher
             ============================================================ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- Wear OS launcher category -->
                <category android:name="android.intent.category.MONKEY" />
            </intent-filter>
        </activity>

        <!-- App Drawer -->
        <activity
            android:name=".AppDrawerActivity"
            android:exported="false"
            android:theme="@style/AppTheme.Transparent" />

        <!-- Wallpaper Picker -->
        <activity
            android:name=".WallpaperPickerActivity"
            android:exported="false" />

    </application>

</manifest>
